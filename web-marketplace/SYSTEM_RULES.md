# System Rules

1. UI must not create a second artwork data source.
2. All artwork data reads/writes go through backend-core APIs.
3. Admin approval state controls public visibility.
4. Payment screens must surface escrow and compliance states.
5. User auth flows are distinct from artwork authentication status.
