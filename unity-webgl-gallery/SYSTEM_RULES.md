# System Rules

1. Unity is read-only for canonical artwork metadata.
2. Unity must not upload duplicate artwork assets as separate records.
3. Unity shows only approved/visible artworks.
4. Assignment logic must map to backend `environment_assignments`.
5. Visibility updates in backend must propagate to gallery sessions.
